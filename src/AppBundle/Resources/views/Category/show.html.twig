{% extends "@App/Main/main.html.twig" %}
{% block title %}Kategorie{% endblock %}
{% block div %}
    <p>Kategorie:</p>
    <a href="{{ path('app_category_add') }}">dodaj kategorie</a>
    <hr>
    {% for category in categories %}
        <div class="cat">

            <p>
                <br>
                <a href="{{ path("app_category_showtask",{"cat":category.id, "id":user.id}) }}">{{ category.name }}
                </a>&nbsp;&nbsp;//&nbsp;
                <a href="{{ path("app_category_delete",{"id":category.id}) }}">usun </a> //

                <a href="{{ path("app_category_edit",{"id":category.id}) }}">zmień nazwe </a>

            </p>
            <br>
        </div>


    {% else %}
        <p>Nie znaleziono kategorii</p>

    {% endfor %}
{% endblock %}